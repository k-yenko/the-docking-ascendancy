name: docking
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch
  - torchvision
  - pymol-open-source
  - pip
  - pip:
    - boltz

variables:
  PYTORCH_ENABLE_MPS_FALLBACK: 1
  CUDA_VISIBLE_DEVICES: ""